// 列表区可以显示所有的物料
// key对应的组件映射关系 

function createEditorConfig(){
  const componentList = []; // 所有组件列表
  const componentMap = {} // 对应的组件映射关系

  return {
    register:()=>{

    }
  }
}
const registerConfig = createEditorConfig()
registerConfig.register({

})